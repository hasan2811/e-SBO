rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Aturan paling terbuka: Izinkan siapa saja untuk membaca dan menulis file.
      allow read, write: if true;
    }
  }
}
