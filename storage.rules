rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access for all paths to any authenticated user.
    // The application logic is responsible for creating correct, secure paths with unguessable names.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
