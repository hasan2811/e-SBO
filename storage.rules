rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow any authenticated user to read and write any file.
    // This trusts the front-end application to handle authorization logic.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
