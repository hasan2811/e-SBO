
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default rule: Allow read and write access for all files
    // only to authenticated users. This simplifies debugging permissions.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
