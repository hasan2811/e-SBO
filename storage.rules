rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow all authenticated users to read and write any file.
    // This rule is open for development and relies on application logic for security.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
