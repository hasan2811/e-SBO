rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // SUPER INSECURE: Open to all reads and writes.
    // This is for debugging purposes as requested, to eliminate permission errors.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
