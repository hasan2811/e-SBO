
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // PUBLIC ACCESS: Allow anyone to read, write, or delete any file.
    // This removes all server-side security for easier front-end development and to resolve permission issues.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
