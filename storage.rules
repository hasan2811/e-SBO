
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Hardcore Rule: Allow all reads and writes to any file.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
