rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow write access only to authenticated users, on files up to 5MB
    match /{allPaths=**} {
      allow read, write: if request.auth != null && request.resource.size < 5 * 1024 * 1024;
    }
  }
}
