rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Aturan permisif: Izinkan baca/tulis ke semua file
    // untuk setiap pengguna yang sudah terautentikasi.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
