rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // This rule allows anyone to read and write to Firebase Storage.
    // This is insecure and should be restricted in a production environment.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
