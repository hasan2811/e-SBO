rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // WARNING: For development only. Allows any authenticated user to read/write all files.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
