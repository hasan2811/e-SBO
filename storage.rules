rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Hardcore Rule: Allow all reads and writes for debugging.
    // This is NOT secure for production.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
