rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Aturan paling terbuka: Izinkan siapa saja untuk membaca dan menulis.
      allow read, write: if true;
    }
  }
}
