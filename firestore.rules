rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: For development only. Allows any authenticated user to read/write all data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
