
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC ACCESS: Allow anyone to read or write to any document.
    // This removes all server-side security for easier front-end development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
