
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Open all reads and writes for any authenticated user.
    // This removes all permission barriers for development.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
