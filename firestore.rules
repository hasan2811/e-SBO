
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Hardcore Rule: Allow all reads and writes to any document.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
