rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Aturan permisif: Izinkan baca/tulis ke semua dokumen
    // untuk setiap pengguna yang sudah terautentikasi.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
